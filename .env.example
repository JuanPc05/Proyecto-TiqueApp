# Base de datos (MariaDB)
DB_ROOT_PASSWORD=
DB_NAME=
DB_USER=
DB_PASSWORD=

# Backend (Spring Boot) — normalmente no necesita variables extra además de las de arriba,
# ya que la URL de conexión se arma en docker-compose.yml a partir de DB_NAME.

# Data service (Python/FastAPI) — igual, la URL de conexión se arma a partir de las variables de arriba.